/*
EDIT>>PREFERENCES>>SQL EDITOR>> 
ABAJO HAY ALGO QUE DICE SAFE MODE, DESACTIVALO, LUEGO CIERRAS Y VUELVES A ABRIR

CUANDO ACABES DE PONER LAS SENTENCIAS VUELVES A HACER LO MISMO PERO PARA ACTIVARLO
*/

ALTER TABLE CORTES_CAJA
ADD COLUMN TURNO INTEGER NOT NULL;

UPDATE CORTES_CAJA
SET TURNO=1;

ALTER TABLE DEVOLUCIONES
ADD COLUMN TURNO INTEGER NOT NULL;

UPDATE DEVOLUCIONES
SET TURNO=1;

ALTER TABLE VENTAS
ADD COLUMN TURNO INTEGER NOT NULL;

UPDATE VENTAS
SET TURNO=1;

ALTER TABLE EGRESOS
ADD COLUMN TURNO INTEGER NOT NULL;

UPDATE EGRESOS
SET TURNO=1;